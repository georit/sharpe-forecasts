# sharpe-forecasts

This is a PWA weather app that I built using the meta-weather API. Users can search for data for a specific location. In addition, the app displays weather info for random locations on launch.
